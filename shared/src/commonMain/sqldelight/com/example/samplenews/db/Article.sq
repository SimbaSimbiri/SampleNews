CREATE TABLE Article(
    title TEXT NOT NULL,
    description TEXT,
    content TEXT,
    date TEXT NOT NULL,
    imageUrl TEXT,
    publisher TEXT NOT NULL
);

insertArticle:
INSERT INTO Article(title, description, content, date, imageUrl, publisher)
VALUES (?,?,?,?,?,?);

selectAllArticles:
SELECT * FROM Article;

removeAllArticles:
DELETE FROM Article;